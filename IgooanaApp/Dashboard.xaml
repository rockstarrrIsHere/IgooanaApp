<phone:PhoneApplicationPage
    x:Class="IgooanaApp.Dashboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:IgooanaApp"
    xmlns:charts="clr-namespace:IgooanaApp.Charts"
    xmlns:localControls="clr-namespace:IgooanaApp.Controls"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True"
   shell:SystemTray.BackgroundColor="{StaticResource AccentBackgroundColor}"
   shell:SystemTray.ForegroundColor="{StaticResource AccentForegroundColor}">

  <local:Layout Background="Transparent">
    <ScrollViewer>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <localControls:DashboardVisitsByCountry/>
        <localControls:DashboardAcquisition Grid.Row="1" MinHeight="500"/>
        <localControls:DashboardPageViews Grid.Row="2" />
      </Grid>

    </ScrollViewer>
  </local:Layout>
  <!--<Grid Background="Transparent">
    <charts:SerialChart x:Name="chart1" DataSource="{Binding Data}" CategoryValueMemberPath="cat1"
                         AxisForeground="White"
                         PlotAreaBackground="Black"
                             MinimumCategoryGridStep="200"
                         GridStroke="DarkGray">
      <charts:SerialChart.Graphs>
        <charts:ColumnGraph ValueMemberPath="val2" Title="Column #2" Brush="#8000FF00" ColumnWidthAllocation="0.8" />
      </charts:SerialChart.Graphs>
    </charts:SerialChart>

  </Grid>-->



</phone:PhoneApplicationPage>