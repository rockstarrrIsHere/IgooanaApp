<UserControl x:Class="IgooanaApp.Controls.DashboardAcquisition"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:charts="clr-namespace:IgooanaApp.Charts"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

  <Grid x:Name="LayoutRoot" Background="Gray">
    <ProgressBar x:Name="ProgressBar" IsIndeterminate="True"/>
    <charts:PieChart x:Name="AcquisitionChart" TitleMemberPath="Title" ValueMemberPath="Value" Visibility="Collapsed"/>
  </Grid>
</UserControl>