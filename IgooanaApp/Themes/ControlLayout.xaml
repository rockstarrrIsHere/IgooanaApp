<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:igooanaControls="clr-namespace:IgooanaApp.WP8.Controls"
    mc:Ignorable="d">
  <Style TargetType="igooanaControls:ControlLayout">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="igooanaControls:ControlLayout">

          <Grid Background="{StaticResource CommonBackgroundBrush}">
            <Grid.RowDefinitions>
              <RowDefinition Height="*"/>
              <RowDefinition Height="14*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{TemplateBinding Title}" Style="{StaticResource ControlTitleText}"/>
            <ContentPresenter Grid.Row="1" Content="{TemplateBinding Content}"/>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
